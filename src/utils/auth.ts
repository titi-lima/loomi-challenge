export const ensureUserLoggedIn = (token: string | undefined) => {
  if (!token) {
    return {
      redirect: {
        destination: "/",
        permanent: false,
      },
    };
  }
  return {
    props: {},
  };
};
